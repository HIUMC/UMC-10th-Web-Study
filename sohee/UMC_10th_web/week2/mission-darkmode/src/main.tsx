import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // 경로를 mission3로 변경!
import './index.css' 

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)